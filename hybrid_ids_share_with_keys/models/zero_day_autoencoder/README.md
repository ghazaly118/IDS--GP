# Zero-Day Autoencoder Artifacts

Put the trained zero-day model files here:

```text
autoencoder_zero_day.keras
scaler.joblib
meta.json
```

These files are produced by the uploaded `zero-day.ipynb` notebook in Kaggle under:

```text
/kaggle/working/zero_day_autoencoder_parquet/
```

Download those three files and copy them into this folder. If the files are missing, the dashboard still runs and the Zero-Day tab will show `UNAVAILABLE`.
